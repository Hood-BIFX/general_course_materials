|  |  |  |  |  |
|---------------|---------------|---------------|---------------|---------------|
| **Category** | **Exceptional** | **Proficient** | **Developing** | **Incomplete/Failing** |
| **Git & Repository** | Repository contains all necessary source files. `.gitignore` is used effectively; no build artifacts, OS junk, or IDE configs present. | All necessary files are present, but includes some extraneous files (e.g., `.DS_Store`, `exe` files). | Missing minor files or repository is disorganized/cluttered with unnecessary data. | Required files are missing, or the code was not submitted via the requested Git protocol. |
| **Execution** | Code runs perfectly "out of the box" without any modifications. All dependencies are handled. | Code runs but requires minor manual adjustments or has specific environment quirks. | Code fails to compile or run due to syntax errors or missing critical logic. | Code does not run at all or is written in the wrong language/format. |
| **Code Quality** | Code is modular (functions/classes used well), consistently formatted, and uses descriptive naming. | Code is mostly neat but contains some "magic numbers," long functions, or inconsistent spacing. | Code is difficult to read, lacks modularity (one giant block), or uses vague variable names (e.g., `x`, `temp1`). | Code is obfuscated, messy, or copied without structure. |
| **Solution Accuracy** | Output is 100% correct and matches all provided test cases/specifications. | Output is mostly correct but contains minor formatting errors or fails one edge case. | Logic is flawed; output is incorrect for several test cases or misses the core objective. | Output is missing, entirely incorrect, or hard-coded to bypass logic. |